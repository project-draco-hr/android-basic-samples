{
  lastGridX=-1;
  lastGridY=-1;
  for (int x=0; x < GRID_SIZE; x++) {
    for (int y=0; y < GRID_SIZE; y++) {
      grid[x][y]=0;
    }
  }
  if (sendMessage) {
    mDrawingActivity.emitClearEvent();
  }
}
